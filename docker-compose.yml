services:
  demo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DEMO_MODE: "1"
      GEMINI_ENABLED: "false"
      RESULTS_SNAPSHOT: "results_snapshot.json"
