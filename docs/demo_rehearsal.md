# Offline demo rehearsal

## Local checklist

1. Build the frozen snapshot with `uv run --locked python scripts/export_snapshot.py`.
2. Start the backend with `DEMO_MODE=1` and `RESULTS_SNAPSHOT=backend/derived/results_snapshot.json`.
3. Start the frontend and follow the email sequence in `docs/showcase_emails.md`.
4. Confirm no Gemini key or network connection is needed.

## Fresh-machine sign-off

Two teammates must each perform this on a fresh clone or separate user profile:

| Run | Person | Date | Result | Notes |
|---|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |

Record the 60-second backup walkthrough after both runs pass.
