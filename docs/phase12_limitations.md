# Phase 12 limitations

## Dev-set review provenance

`dev_labels/dev_labels.json` was produced by a single labeler. It has 40
evidence-noted labels and is useful for local regression checks, but it does
not satisfy the Phase 12 requirement for two independent labelers with a
reconciled overlap subset. The Phase 12 dev-set checklist item remains
unchecked until that review is completed.
